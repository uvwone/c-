#include <stdio.h>

using namespace std;

int main()
{
    int a = 0, i, x, y;
    scanf("%d %d", &x, &y);

    for (i = 1; i <= x && i <= y; i++) {
        if (x % i == 0 && y % i == 0) {
            a = i;
        }
    }
    printf("%d", a);
    return 0;
}
